<p>all posts</p>

<% @posts.each do |post| %>
    <%= link_to (image_tag post.photo.url(:thumb)), post_path(post) %>
    <p><%= link_to post.caption, post_path(post) %></p>
    <%= link_to 'Hide', hide_post_path(post.id) if current_user.admin? %>
<% end %>

<% @posts.each do |post| %>
    <%= link_to (image_tag post.photo.url(:thumb)), post_path(post) %>
    <p><%= link_to post.caption, post_path(post) %></p>
    <%= link_to 'Unhide', unhide_post_path(post.id) if current_user.admin? %>
<% end %>
